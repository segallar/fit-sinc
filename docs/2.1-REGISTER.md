# 2.1 — Саморегистрация (`/register`)

> **ID roadmap:** **2.1** (фаза 5b.3) · **2.2** — тесты · **2.6** — подтверждение email и invite  
> **Статус:** **MVP реализован** (2026-05-26): форма, rate limit, auto-login → `/app/`  
> **Связано:** [PLAN.md](PLAN.md) · [5b-DECISIONS.md](5b-DECISIONS.md) · [ARCHITECTURE.md](ARCHITECTURE.md)

---

## Цель

Позволить новому пользователю создать аккаунт **без админки и CLI**, войти в кабинет и дальше подключить Hammerhead / Garmin в **Settings**.

**Не цель 2.1:** подтверждение email, invite-only, captcha, OAuth — см. фазы ниже.

---

## Решения (зафиксировано)

| # | Решение | Выбор |
|---|---------|--------|
| R1 | URL регистрации | `GET/POST /register` |
| R2 | URL входа с главной | кнопка **Login** → `/app/login` |
| R3 | Sign up с главной | кнопка **Sign up** → `/register` |
| R4 | Поля формы | email, display_name (опц.), password ×2; timezone — дефолт в БД, в **Settings** |
| R5 | Slug | **автоген** из email (`user`, `user-2`, …) — в UI не показываем |
| R6 | После успеха | **auto-login** → redirect **`/app/`** |
| R7 | Флаг prod | `REGISTRATION_OPEN=false` по умолчанию |
| R8 | Подтверждение email | **не в 2.1** → **2.1e** (этот документ, § Email) |

---

## Реализовано (MVP 2.1)

| Компонент | Путь |
|-----------|------|
| Роуты | [`getsync/web/register_routes.py`](../getsync/web/register_routes.py) |
| Slug | [`getsync/users/slug.py`](../getsync/users/slug.py) |
| Rate limit | [`getsync/web/rate_limit.py`](../getsync/web/rate_limit.py) — 5 POST / 15 мин / IP |
| Шаблон | [`getsync/web/templates/pages/site/register.html`](../getsync/web/templates/pages/site/register.html) |
| Главная | [`getsync/web/templates/pages/site/home.html`](../getsync/web/templates/pages/site/home.html) — Login + Sign up |
| Auth | `/register` публичный в [`auth.py`](../getsync/web/auth.py) |
| Config | `REGISTRATION_OPEN` в [`.env.example`](../.env.example) |

### Поведение

- `REGISTRATION_OPEN=false` → `GET /register` → страница «регистрация закрыта» (403).
- `REGISTRATION_OPEN=true` → форма; дубликат email → ошибка; слабый пароль / mismatch → ошибка.
- Уже залогинен → `303 /app/`.
- Новый user: `is_admin=0`, каталог `data/users/{slug}/` создаётся при первом sync/OAuth.

### Онбординг после регистрации (ручной, без 2.6)

1. `/app/` — дашборд, баннер подключений (**1.8**).
2. **Settings** → Hammerhead OAuth (`hammerhead_user_id` для webhook).
3. Garmin — **первый** login пока через CLI или import cookies (**1.2**); refresh/status в UI.

---

## Email — отдельная подфаза **2.1e** (не сделано)

### Проблема

Без проверки email любой может зарегистрировать чужой адрес → письма/уведомления (будущие), восстановление пароля, доверие к аккаунту.

### Целевое поведение

```text
POST /register
  → users.email_verified = 0, status = pending
  → письмо со ссылкой /verify-email?token=…
  → до клика: вход запрещён ИЛИ вход есть, но sync/HH disabled (выбрать при проектировании)
POST /verify-email
  → token одноразовый, TTL 24–72 ч
  → email_verified = 1, status = active
```

### Варианты политики до подтверждения

| Вариант | Плюсы | Минусы |
|---------|--------|--------|
| **A. Нет входа до verify** | проще безопасность | хуже UX после register |
| **B. Вход есть, sync выкл.** | как сейчас auto-login + баннер «подтвердите почту» | нужен guard в sync/webhook |
| **C. Grace period 24 ч** | компромисс | сложнее |

**Рекомендация для GetSync:** **B** — после register auto-login на `/app/` с flash «Проверьте почту»; webhook/sync для user с `email_verified=0` не выполнять.

### Задачи 2.1e (оценка ~3–4 вечера)

> **Как отправлять почту, провайдеры, DNS, шаблоны:** [2.1e-EMAIL.md](2.1e-EMAIL.md)

| ID | Задача |
|----|--------|
| 2.1e.0 | `getsync/mail/` + backends (`null`, SMTP/API) |
| 2.1e.1 | Миграция SQLite: `email_verified`, `email_verify_token_hash`, `email_verify_sent_at` |
| 2.1e.2 | Prod: Resend/SendGrid + SPF/DKIM на getsync.me |
| 2.1e.3 | `POST /register` → письмо M1 + `GET /verify-email?token=` |
| 2.1e.4 | Resend verify (M2) + rate limit |
| 2.1e.5 | Guard sync + banner в кабинете |
| 2.1e.6 | Тесты + i18n subject/body |

### Связь с roadmap

- Полный пакет (invite, captcha, политика пароля) остаётся в **2.6** ([PLAN.md](PLAN.md)).
- **2.1e** — минимальный верификационный контур — [2.1e-EMAIL.md](2.1e-EMAIL.md); можно выпустить раньше 2.6.

---

## Деплой и ops

| Шаг | Действие |
|-----|----------|
| Staging | `REGISTRATION_OPEN=true`, пройти register → settings → HH OAuth |
| Production | включать `true` только когда нужны публичные регистрации |
| Hammerhead | после появления users — у каждого свой OAuth; webhook по `userId` |
| Спам | rate limit в коде; при росте — **2.6** captcha |

---

## Тесты

| ID | Содержание | Статус |
|----|------------|--------|
| **2.2** | register open/closed, duplicate email, auto-login, rate limit | ✅ `tests/test_register.py` |

---

## Чеклист готовности 2.1 (MVP)

- [x] `/register` GET/POST
- [x] `REGISTRATION_OPEN`
- [x] slug autogen
- [x] auto-login → `/app/`
- [x] главная: Login + Sign up
- [x] rate limit IP
- [ ] **2.1e** email verify
- [x] **2.2** тесты регистрации (`tests/test_register.py`)
- [ ] prod: осознанно включить `REGISTRATION_OPEN`
