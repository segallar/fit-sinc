#!/usr/bin/env bash
# UI uses Bootstrap 5 from CDN in layouts/base.html.
# getsync/web/static/app.css — theme overrides only (committed).
echo "Bootstrap UI: no CSS build step (app.css is hand-maintained)"
